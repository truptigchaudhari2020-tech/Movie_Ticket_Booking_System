# Movie_Ticket_Booking_System
A console-based movie ticket booking system is a text-driven application that operates entirely within the terminal using a simple command-line interface (CLI). Users interact with the system by typing numeric choices or text commands to view ASCII-based seat layouts, select shows, and simulate ticket generation without any graphical interface.
